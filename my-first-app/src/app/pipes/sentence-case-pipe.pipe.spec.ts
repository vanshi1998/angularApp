import { SentenceCasePipePipe } from './sentence-case-pipe.pipe';

describe('SentenceCasePipePipe', () => {
  it('create an instance', () => {
    const pipe = new SentenceCasePipePipe();
    expect(pipe).toBeTruthy();
  });
});
